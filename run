#! /bin/sh

git add .
echo "Enter Commit Message: $name"
read commitMessage
git commit -m "$commitMessage $(git describe --abbrev=0 --tags)"
echo "Current Tag is : " $(git describe --abbrev=0 --tags)
echo "Enter the new tag "
read newTag
git tag '$newTag'
git push --tag

