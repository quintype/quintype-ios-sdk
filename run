#! /bin/sh

git add .
echo "Enter Commit Message: $name"
read commitMessage
git commit -m "$commitMessage"
currentTag=$(git describe --abbrev=0 --tags)
newTag=0.01 + $(git describe --abbrev=0 --tags) | bc -l
echo "albin" $newTag


